from setuptools import setup

setup(name='components',
      version='0.1',
      description='Parse delimited files and sort the data',
      author='Vishwaram Maharaj',
      author_email='vishwaramm@gmail.com',
      license='MIT',
      packages=['components'],
      zip_safe=False,
      test_suite='unittest',
      install_requres=['enum'])
